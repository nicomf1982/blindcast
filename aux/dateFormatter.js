'use strict'

const formatter = function (obj){
  // obj.forEach(e => {
  //   UNIX_Timestamp
  //     e.createdAt = new Date(e.createdAt)
  // });
  // // const res = obj.map( e => new Date(e.createdAt))
  return obj
}

module.exports = formatter